<div class="row marketing">
    <div class="col-lg-12">
        <h4>Skills and expertise</h4>
        <p>
            With more than thirty years experience, I've had the pleasure to work in the wonderful world
            of software development. As a result, I can offer services and consultancy in a wide range.
        </p>

        <div class="row skills">
            <div class="col-lg-6">
                {{#each model as |skill index|}}
                    {{#if (lt index (half-of model.length))}}
                        <p>{{fa-icon 'wrench' class='text-success'}}&nbsp; {{skill.name}}</p>
                    {{/if}}
                {{/each}}
            </div>

            <div class="col-lg-6">
                {{#each model as |skill index|}}
                    {{#if (gte index (half-of model.length))}}
                        <p>{{fa-icon 'wrench' class='text-success'}}&nbsp; {{skill.name}}</p>
                    {{/if}}
                {{/each}}
            </div>
        </div>
    </div>
</div>
