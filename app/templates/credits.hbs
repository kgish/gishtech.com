<div class="row marketing">
    <div class="col-lg-12">
        <h4>Credits</h4>
        <p>
            This website would never have been possible without the help of the following fantastic open source tools and utilities.
        </p>

        <div class="row credits">
            <div class="col-lg-6">
                {{#each model as |credit index|}}
                    {{#if (lt index (half-of model.length))}}
                        <p>{{fa-icon 'star' class='text-success'}}&nbsp; <a href="http://{{credit.url}}" target="_blank">{{credit.name}}</a></p>
                    {{/if}}
                {{/each}}
            </div>

            <div class="col-lg-6">
                {{#each model as |credit index|}}
                    {{#if (gte index (half-of model.length))}}
                        <p>{{fa-icon 'star' class='text-success'}}&nbsp; <a href="http://{{credit.url}}" target="_blank">{{credit.name}}</a></p>
                    {{/if}}
                {{/each}}
            </div>
        </div>
    </div>
</div>
