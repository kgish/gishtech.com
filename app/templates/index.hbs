<div class="row marketing">
    <div class="col-lg-12">
        {{#each model as |intro|}}
            <h4 class="with-fa">{{fa-icon 'plus-square' class='text-success'}}&nbsp; {{intro.title}}</h4>
            <p>{{intro.description}}</p>
        {{/each}}
    </div>
</div>