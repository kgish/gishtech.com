import { halfOf } from 'gishtech/helpers/half-of';
import { module, test } from 'qunit';

module('Unit | Helper | half of');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = halfOf([42]);
  assert.ok(result);
});
