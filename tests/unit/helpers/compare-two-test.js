import { compareTwo } from 'gishtech/helpers/compare-two';
import { module, test } from 'qunit';

module('Unit | Helper | compare two');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = compareTwo([42]);
  assert.ok(result);
});
