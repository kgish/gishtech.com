import { skillPropertyScore } from 'gishtech/helpers/skill-property-score';
import { module, test } from 'qunit';

module('Unit | Helper | skill property score');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = skillPropertyScore([42]);
  assert.ok(result);
});
